<configuration>
    <!-- Reduces the spammy logback logging during startup -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <appender name="secure_log" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>team-logs.nais-system:5170</destination>
        <keepAliveDuration>5 minutes</keepAliveDuration>
        <reconnectionDelay>1 minute</reconnectionDelay>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>
                {"google_cloud_project":"${GOOGLE_CLOUD_PROJECT}","nais_namespace_name":"${NAIS_NAMESPACE}","nais_pod_name":"${NAIS_POD_NAME}","nais_container_name":"${NAIS_APP_NAME}"}
            </customFields>
            <includeContext>false</includeContext>
        </encoder>
    </appender>

    <logger name="securelog" level="info" additivity="false">
        <appender-ref ref="secure_log" />
    </logger>

    <root level="INFO">
        <appender-ref ref="STDOUT_JSON"/>
    </root>

    <!-- Stuff we wanna squelch, use sparingly -->
    <logger name="ch.qos.logback" level="WARN"/>
    <logger name="io.swagger.codegen.v3.generators" level="WARN"/>
    <logger name="org.apache.kafka" level="WARN"/>

    <!-- Debug logs for dev -->
    <logger name="no.nav.helse" level="DEBUG"/>
    <logger name="io.ktor.server" level="DEBUG"/>
    <logger name="io.ktor.http" level="DEBUG"/>
    <logger name="io.netty" level="DEBUG"/>

    <appender name="STDOUT_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>x_request_id</includeMdcKeyName>
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
        </encoder>
    </appender>
</configuration>
